#/usr/bin/env bash

# correct value of tStar: 1e+0

folder=./data/UC_Data/T-Ramp/
#par=BSPar-1LD.txt

for size in 10 20 50 75 100 200
do
for (( seed=1 ; seed<6 ; seed++ ))
do

if [ -z "$par" ]; then
printf "[${size}_0_${seed}]: "
else
printf "[${size}_0_${seed} $par]: "
fi

./LDS_UC_test $folder${size}_0_${seed}_w.nc4 $par

retVal=$?
if [ $retVal -ne 0 ]; then
    exit 1
fi

done
done
